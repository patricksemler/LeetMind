{
  "name": "@leetmind/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "migrate": "tsx src/migrate.ts up",
    "migrate:status": "tsx src/migrate.ts status",
    "migrate:create": "tsx src/migrate.ts create",
    "seed": "tsx src/migrate.ts up",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p ."
  },
  "dependencies": {
    "@leetmind/shared": "workspace:*",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
