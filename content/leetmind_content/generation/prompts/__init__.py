"""Prompt builders for `leetmind_content.generation`, versioned by module (`v1.py` ->
`PROMPT_VERSION = "v1"`, future revisions get their own `v2.py` etc. rather than mutating `v1`
in place, so old `model_runs.prompt_version` values stay meaningful)."""
